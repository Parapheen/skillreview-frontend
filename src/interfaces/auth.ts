export interface ISteamResponse {
    assocHandle: string
    claimedId: string
    identity: string
    mode: string
    ns: string
    opEndpoint: string
    responseNonce: string
    returnTo: string
    sig: string
    signed: string
}